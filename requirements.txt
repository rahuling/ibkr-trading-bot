# IBKR connection — ib_async is the maintained fork of the deprecated ib_insync
ib-async>=0.9.0

# Telegram bot (async, v20+)
python-telegram-bot>=20.7

# Scheduling — must configure timezone=America/New_York explicitly
APScheduler>=3.10.4

# Config validation
pydantic>=2.5.0
PyYAML>=6.0.1

# Environment variables
python-dotenv>=1.0.0

# Async SQLite
aiosqlite>=0.19.0

# Timezone support for APScheduler
pytz>=2024.1
